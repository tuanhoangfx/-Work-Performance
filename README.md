# Infi Project - Task Manager

## 🚨 Troubleshooting

### Lỗi: Không thể thêm bình luận

**Nguyên nhân:** Lỗi này xảy ra do cơ sở dữ liệu của bạn chưa có bảng `task_comments` để lưu trữ bình luận.

**✅ Cách khắc phục:** Chạy lệnh SQL sau trong **SQL Editor** của dự án Supabase để tạo bảng và các chính sách bảo mật cần thiết.

```sql
-- TẠO BẢNG MỚI: task_comments
-- Bảng này sẽ lưu trữ các bình luận cho mỗi công việc.
CREATE TABLE public.task_comments (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  created_at timestamptz NOT NULL DEFAULT now(),
  task_id bigint NOT NULL REFERENCES public.tasks(id) ON DELETE CASCADE,
  user_id uuid NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE,
  content text NOT NULL CHECK (content <> '')
);

COMMENT ON TABLE public.task_comments IS 'Lưu trữ các bình luận cho công việc.';
-- Tạo index để tăng tốc độ truy vấn
CREATE INDEX ON public.task_comments (task_id);
CREATE INDEX ON public.task_comments (user_id);

-- Bật RLS và thêm chính sách cho bảng task_comments
ALTER TABLE public.task_comments ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Người dùng đã xác thực có thể xem tất cả bình luận." ON public.task_comments
  FOR SELECT USING (auth.role() = 'authenticated');

CREATE POLICY "Người dùng có thể thêm bình luận." ON public.task_comments
  FOR INSERT WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Người dùng có thể sửa bình luận của chính họ." ON public.task_comments
  FOR UPDATE USING (auth.uid() = user_id);

CREATE POLICY "Người dùng có thể xóa bình luận của chính họ." ON public.task_comments
  FOR DELETE USING (auth.uid() = user_id);

CREATE POLICY "Admin có thể quản lý tất cả bình luận." ON public.task_comments
  FOR ALL USING (public.is_admin());
```

---

### Lỗi: `new row violates row-level security policy for table "tasks"`

**Nguyên nhân:** Lỗi này xảy ra khi một người dùng (ví dụ: vai trò 'employee') cố gắng tạo một công việc và giao cho người khác. Chính sách bảo mật hiện tại không cho phép hành động này.

**✅ Cách khắc phục:** Cập nhật chính sách RLS cho bảng `tasks` bằng cách chạy đoạn mã SQL sau trong **SQL Editor** của dự án Supabase. Đoạn mã này sẽ thay thế chính sách cũ bằng các chính sách mới, rõ ràng và an toàn hơn.

```sql
-- Tập lệnh này khắc phục lỗi bằng cách thay thế chính sách cũ, không rõ ràng bằng các chính sách mới, mạnh mẽ hơn.

-- 1. Xóa các chính sách cũ có thể gây xung đột để đảm bảo thiết lập sạch.
DROP POLICY IF EXISTS "Users can manage tasks they are assigned to or created." ON public.tasks;
DROP POLICY IF EXISTS "Người dùng có thể quản lý (xem, tạo, sửa, xóa) các công việc của chính họ." ON public.tasks;
DROP POLICY IF EXISTS "Admin có thể quản lý tất cả các công việc." ON public.tasks;
DROP POLICY IF EXISTS "Admin can manage all tasks." ON public.tasks;


-- 2. Tạo chính sách mới, cụ thể cho việc TẠO (INSERT) công việc.
-- Người dùng có thể tạo công việc cho bất kỳ ai, miễn là họ được đánh dấu là người tạo.
CREATE POLICY "Người dùng có thể tạo công việc."
ON public.tasks FOR INSERT
WITH CHECK (auth.uid() = created_by);


-- 3. Tạo chính sách cho việc XEM (SELECT), CẬP NHẬT (UPDATE), và XÓA (DELETE) công việc.
-- Người dùng có thể truy cập một công việc nếu họ là người tạo HOẶC người được giao.
CREATE POLICY "Người dùng có thể xem, cập nhật, xóa công việc được giao hoặc do họ tạo."
ON public.tasks FOR SELECT, UPDATE, DELETE
USING (auth.uid() = user_id OR auth.uid() = created_by);


-- 4. Tạo lại chính sách ghi đè cho admin.
-- Admin có thể làm mọi thứ.
CREATE POLICY "Admin có thể quản lý tất cả các công việc."
ON public.tasks FOR ALL
USING (public.is_admin());
```

---

### Lỗi: Avatar người tạo không hiển thị và không thể giao việc cho người khác

**Nguyên nhân:** Lỗi này xảy ra do Chính sách Bảo mật Cấp độ Hàng (RLS) của cơ sở dữ liệu quá nghiêm ngặt. Người dùng có vai trò 'employee' chỉ được phép xem hồ sơ của chính họ. Điều này ngăn họ xem thông tin của người tạo công việc hoặc xem những người dùng khác trong danh sách để giao việc.

**✅ Cách khắc phục:** Cập nhật chính sách RLS cho bảng `profiles` để cho phép bất kỳ người dùng đã đăng nhập nào cũng có thể xem tất cả hồ sơ. Điều này thường là an toàn cho một công cụ nội bộ của công ty.

Chạy các lệnh SQL sau trong **SQL Editor** của dự án Supabase của bạn:

```sql
-- Xóa chính sách SELECT cũ và quá hạn chế trên bảng profiles
DROP POLICY IF EXISTS "Người dùng có thể xem hồ sơ của chính họ." ON public.profiles;

-- Tạo chính sách mới cho phép bất kỳ người dùng đã đăng nhập nào cũng có thể xem tất cả hồ sơ
CREATE POLICY "Authenticated users can view all profiles." 
ON public.profiles FOR SELECT 
USING (auth.role() = 'authenticated');
```

---

### Lỗi: `invalid input value for enum task_status: "cancelled"`

**Nguyên nhân:** Lỗi này xảy ra do schema cơ sở dữ liệu của bạn chưa được cập nhật để hỗ trợ trạng thái "cancelled" (đã hủy) cho các công việc. Điều này có thể xảy ra nếu bạn đã thiết lập dự án bằng phiên bản cũ hơn của tập lệnh thiết lập.

**✅ Cách khắc phục:** Chạy lệnh SQL sau trong **SQL Editor** của dự án Supabase để thêm trạng thái còn thiếu:

```sql
ALTER TYPE public.task_status ADD VALUE IF NOT EXISTS 'cancelled';
```
*Lưu ý: `IF NOT EXISTS` sẽ ngăn lỗi nếu trạng thái đã tồn tại.*

---

### Lỗi: `new row violates row-level security policy for table "profiles"`

**Nguyên nhân:** Lỗi này xảy ra khi thiết lập cơ sở dữ liệu của bạn thiếu một chính sách bảo mật bắt buộc, ngăn người dùng mới tự tạo hồ sơ của họ.

**✅ Cách khắc phục:** Chạy lệnh SQL sau trong **SQL Editor** để thêm chính sách bảo mật cần thiết:

```sql
-- Cho phép người dùng tự chèn hồ sơ của chính họ
CREATE POLICY "Người dùng có thể tự chèn hồ sơ của mình." 
ON public.profiles FOR INSERT 
WITH CHECK (auth.uid() = id);
```
*Lưu ý: Tập lệnh thiết lập đầy đủ bên dưới đã bao gồm chính sách này.*

---

### Cập nhật: Thêm Bảng Nhật ký Hoạt động (Activity Log)

**Nguyên nhân:** Để triển khai tính năng Nhật ký Hoạt động, cần có một bảng mới trong cơ sở dữ liệu để lưu trữ các sự kiện này.

**✅ Cách khắc phục:** Chạy lệnh SQL sau trong **SQL Editor** để tạo bảng `activity_logs` và các chính sách bảo mật liên quan.

```sql
-- TẠO BẢNG MỚI: activity_logs
-- Bảng này sẽ lưu trữ các bản ghi về hành động của người dùng như tạo, cập nhật, xóa công việc.
CREATE TABLE public.activity_logs (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  created_at timestamptz NOT NULL DEFAULT now(),
  user_id uuid NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE,
  task_id bigint REFERENCES public.tasks(id) ON DELETE SET NULL,
  action text NOT NULL,
  details jsonb
);

COMMENT ON TABLE public.activity_logs IS 'Ghi lại các hoạt động của người dùng trong ứng dụng.';
-- Tạo index để tăng tốc độ truy vấn
CREATE INDEX ON public.activity_logs (user_id);
CREATE INDEX ON public.activity_logs (task_id);

-- Bật RLS và thêm chính sách cho bảng activity_logs
ALTER TABLE public.activity_logs ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Người dùng đã xác thực có thể xem tất cả logs." ON public.activity_logs
  FOR SELECT USING (auth.role() = 'authenticated');
  
CREATE POLICY "Người dùng có thể tạo log cho hành động của chính họ." ON public.activity_logs
  FOR INSERT WITH CHECK (auth.uid() = user_id);
  
CREATE POLICY "Admin có thể quản lý tất cả logs." ON public.activity_logs
  FOR ALL USING (public.is_admin());
```

---

### CẬP NHẬT: Thêm Hệ thống Thông báo

**Nguyên nhân:** Để bật tính năng thông báo theo thời gian thực cho các sự kiện như giao việc và bình luận mới, cần có một bảng `notifications` chuyên dụng và các hàm logic trong cơ sở dữ liệu.

**✅ Cách khắc phục:** Chạy các tập lệnh SQL sau theo thứ tự trong **SQL Editor** của dự án Supabase của bạn.

**Bước 1: Tạo bảng `notifications`**
```sql
-- TẠO BẢNG MỚI: notifications
-- Bảng này sẽ lưu trữ thông báo cho người dùng về các sự kiện quan trọng.
CREATE TABLE public.notifications (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  created_at timestamptz NOT NULL DEFAULT now(),
  user_id uuid NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE, -- Người nhận
  actor_id uuid REFERENCES public.profiles(id) ON DELETE SET NULL, -- Người thực hiện hành động
  type text NOT NULL, -- Ví dụ: 'new_task_assigned', 'new_comment'
  data jsonb, -- Dữ liệu liên quan như task_id, task_title
  is_read boolean NOT NULL DEFAULT false
);

COMMENT ON TABLE public.notifications IS 'Lưu trữ thông báo cho người dùng.';

-- Bật RLS và thêm chính sách cho bảng notifications
ALTER TABLE public.notifications ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view their own notifications." ON public.notifications
  FOR SELECT USING (auth.uid() = user_id);

CREATE POLICY "Users can mark their own notifications as read." ON public.notifications
  FOR UPDATE USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Admins can manage all notifications." ON public.notifications
  FOR ALL USING (public.is_admin());
```

**Bước 2: Tạo hàm và trigger cho việc giao việc mới**
```sql
-- HÀM: create_new_task_assignment_notification
-- Hàm này sẽ tạo thông báo khi một công việc được tạo hoặc được giao lại cho người khác.
CREATE OR REPLACE FUNCTION public.create_new_task_assignment_notification()
RETURNS TRIGGER
LANGUAGE plpgsql
SECURITY DEFINER SET search_path = public
AS $$
DECLARE
  assigner_name text;
BEGIN
  -- Lấy tên của người dùng đang thực hiện hành động
  SELECT full_name INTO assigner_name FROM profiles WHERE id = auth.uid();

  -- Chèn thông báo cho người dùng mới được giao,
  -- chỉ khi họ không tự giao việc cho chính mình.
  IF NEW.user_id IS NOT NULL AND NEW.user_id <> auth.uid() THEN
    INSERT INTO public.notifications (user_id, actor_id, type, data)
    VALUES (
      NEW.user_id,
      auth.uid(),
      'new_task_assigned',
      jsonb_build_object(
        'task_id', NEW.id,
        'task_title', NEW.title,
        'assigner_name', assigner_name
      )
    );
  END IF;

  RETURN NEW;
END;
$$;

-- TRIGGER: on_new_task_created_for_other
-- Kích hoạt khi một công việc MỚI được tạo.
CREATE TRIGGER on_new_task_created_for_other
  AFTER INSERT ON public.tasks
  FOR EACH ROW
  EXECUTE PROCEDURE public.create_new_task_assignment_notification();

-- TRIGGER: on_task_reassigned
-- Kích hoạt khi trường `user_id` (người được giao) của một công việc được CẬP NHẬT.
CREATE TRIGGER on_task_reassigned
  AFTER UPDATE OF user_id ON public.tasks
  FOR EACH ROW
  WHEN (OLD.user_id IS DISTINCT FROM NEW.user_id)
  EXECUTE PROCEDURE public.create_new_task_assignment_notification();
```

**Bước 3: Tạo hàm và trigger cho bình luận mới**
```sql
-- HÀM: create_new_comment_notification
-- Hàm này sẽ tạo thông báo khi có bình luận mới trên một công việc.
CREATE OR REPLACE FUNCTION public.create_new_comment_notification()
RETURNS TRIGGER
LANGUAGE plpgsql
SECURITY DEFINER SET search_path = public
AS $$
DECLARE
  task_info record;
  commenter_name text;
BEGIN
  -- Lấy thông tin về người sở hữu, người tạo và tiêu đề công việc
  SELECT user_id, created_by, title INTO task_info FROM tasks WHERE id = NEW.task_id;
  -- Lấy tên của người bình luận
  SELECT full_name INTO commenter_name FROM profiles WHERE id = NEW.user_id;

  -- Gửi thông báo cho NGƯỜI ĐƯỢC GIAO CÔNG VIỆC nếu họ không phải là người bình luận
  IF task_info.user_id IS NOT NULL AND task_info.user_id <> NEW.user_id THEN
    INSERT INTO public.notifications (user_id, actor_id, type, data)
    VALUES (
      task_info.user_id,
      NEW.user_id,
      'new_comment',
      jsonb_build_object(
        'task_id', NEW.task_id,
        'task_title', task_info.title,
        'commenter_name', commenter_name
      )
    );
  END IF;

  -- Gửi thông báo cho NGƯỜI TẠO CÔNG VIỆC nếu họ không phải là người bình luận VÀ không phải là người được giao (để tránh thông báo kép)
  IF task_info.created_by IS NOT NULL AND task_info.created_by <> NEW.user_id AND task_info.created_by <> task_info.user_id THEN
    INSERT INTO public.notifications (user_id, actor_id, type, data)
    VALUES (
      task_info.created_by,
      NEW.user_id,
      'new_comment',
      jsonb_build_object(
        'task_id', NEW.task_id,
        'task_title', task_info.title,
        'commenter_name', commenter_name
      )
    );
  END IF;

  RETURN NEW;
END;
$$;

-- TRIGGER: on_new_comment
-- Kích hoạt khi có một bình luận MỚI được thêm vào.
CREATE TRIGGER on_new_comment
  AFTER INSERT ON public.task_comments
  FOR EACH ROW
  EXECUTE PROCEDURE public.create_new_comment_notification();
```

---

## 📖 Project Overview

Infi Project Task Manager is a modern, elegant internal tool designed for managing team member tasks through an intuitive, Kanban-style interface. It focuses on a smooth user experience, clear data presentation, and a dedicated dashboard for managers.

## ✨ Key Features

*   **Kanban Board**: An interactive board for team members to view and manage their tasks, organized by status ('To Do', 'In Progress', 'Done').
*   **Task Management**: Users can create, update, and delete their own tasks. Each task can have a title, description, status, priority, and due date.
*   **Task Comments**: A full commenting system on each task to facilitate team communication.
*   **File Attachments**: Attach files (images, documents) to tasks for better context and information sharing, with support for direct pasting from the clipboard.
*   **Real-time Notifications**: Get notified instantly when you're assigned a task or when someone comments on your tasks.
*   **Priority and Status**: Clearly defined priorities ('Low', 'Medium', 'High') and statuses help in organizing and tracking work efficiently.
*   **Comprehensive Manager Dashboard**:
    *   A separate view for users with an 'admin' role.
    *   View a list of all team members and select any team member to view their task board.
    *   Full CRUD (Create, Read, Update, Delete) functionality for tasks for any team member.
    *   Includes an 'Overall Summary' view showing aggregated statistics for all tasks in the system.
*   **Secure Authentication & Authorization**: Powered by Supabase Auth with Row Level Security (RLS) to ensure users can only access their own data, while managers have full access.
*   **Customizable UI**: Switch between Light/Dark modes with beautiful gradient color themes.
*   **Multi-language Support**: English, Vietnamese (Tiếng Việt), and Thai (ไทย).
*   **Responsive Design**: A fully responsive layout for all screen sizes.
*   **In-App User Guide**: A comprehensive guide available directly within the application.

## 💻 Tech Stack

-   **Frontend**: React, TypeScript, Tailwind CSS
-   **Backend**: Supabase (Database, Auth, Storage, RLS, Realtime)
-   **State Management**: React Context API & Component State

---

## 🛠️ Setup Instructions

To get the application running with your own Supabase backend, follow these steps.

### 1. Create a Supabase Project

1.  Go to [supabase.com](https://supabase.com/) and sign in.
2.  Create a new project. Save your **Project URL** and **anon key**.

### 2. Database Schema Setup

Go to the **SQL Editor** in your Supabase project dashboard and run the following script. This single script will set up everything you need.

```sql
-- 1. TẠO CÁC KIỂU ENUM TÙY CHỈNH
-- Điều này giúp đảm bảo dữ liệu nhất quán cho trạng thái và mức độ ưu tiên của công việc.
CREATE TYPE public.task_status AS ENUM ('todo', 'inprogress', 'done', 'cancelled');
CREATE TYPE public.user_role AS ENUM ('employee', 'admin');
CREATE TYPE public.task_priority AS ENUM ('low', 'medium', 'high');


-- 2. BẢNG PROFILES
-- Lưu trữ thông tin người dùng công khai, được liên kết với bảng `auth.users` của Supabase.
CREATE TABLE public.profiles (
  id uuid NOT NULL PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  updated_at timestamptz,
  full_name text,
  avatar_url text,
  role public.user_role NOT NULL DEFAULT 'employee'
);
-- Thêm comment cho các cột để dễ hiểu
COMMENT ON TABLE public.profiles IS 'Lưu trữ thông tin hồ sơ công khai cho người dùng.';


-- 3. BẢNG TASKS
-- Bảng chính để lưu trữ tất cả các công việc.
CREATE TABLE public.tasks (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id uuid NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE,
  created_by uuid REFERENCES public.profiles(id) ON DELETE SET NULL,
  created_at timestamptz NOT NULL DEFAULT now(),
  updated_at timestamptz NOT NULL DEFAULT now(),
  title text NOT NULL,
  description text,
  status public.task_status NOT NULL DEFAULT 'todo',
  priority public.task_priority NOT NULL DEFAULT 'medium',
  due_date date
);
-- Thêm comment
COMMENT ON TABLE public.tasks IS 'Lưu trữ các công việc cho người dùng.';
-- Tạo index để tăng tốc độ truy vấn
CREATE INDEX ON public.tasks (user_id);
CREATE INDEX ON public.tasks (status);


-- 4. BẢNG TASK_ATTACHMENTS
-- Bảng để lưu trữ tệp đính kèm cho công việc.
CREATE TABLE public.task_attachments (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  task_id bigint NOT NULL REFERENCES public.tasks(id) ON DELETE CASCADE,
  user_id uuid NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE,
  file_name text NOT NULL,
  file_path text NOT NULL,
  file_type text,
  file_size bigint,
  created_at timestamptz NOT NULL DEFAULT now()
);
COMMENT ON TABLE public.task_attachments IS 'Lưu trữ các tệp đính kèm cho công việc.';
CREATE INDEX ON public.task_attachments (task_id);


-- 5. BẢNG TASK_TIME_LOGS
-- Lưu trữ các bản ghi thời gian cho mỗi công việc.
CREATE TABLE public.task_time_logs (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    task_id bigint NOT NULL REFERENCES public.tasks(id) ON DELETE CASCADE,
    user_id uuid NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE,
    start_time timestamptz NOT NULL,
    end_time timestamptz,
    created_at timestamptz NOT NULL DEFAULT now()
);
COMMENT ON TABLE public.task_time_logs IS 'Lưu trữ các bản ghi thời gian làm việc cho các công việc.';
CREATE INDEX ON public.task_time_logs (task_id);
CREATE INDEX ON public.task_time_logs (user_id);


-- 6. BẢNG ACTIVITY_LOGS
-- Lưu trữ các bản ghi về hành động của người dùng trong ứng dụng.
CREATE TABLE public.activity_logs (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  created_at timestamptz NOT NULL DEFAULT now(),
  user_id uuid NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE,
  task_id bigint REFERENCES public.tasks(id) ON DELETE SET NULL,
  action text NOT NULL,
  details jsonb
);
COMMENT ON TABLE public.activity_logs IS 'Ghi lại các hoạt động của người dùng trong ứng dụng.';
CREATE INDEX ON public.activity_logs (user_id);
CREATE INDEX ON public.activity_logs (task_id);


-- 7. BẢNG TASK_COMMENTS
-- Lưu trữ các bình luận cho công việc.
CREATE TABLE public.task_comments (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  created_at timestamptz NOT NULL DEFAULT now(),
  task_id bigint NOT NULL REFERENCES public.tasks(id) ON DELETE CASCADE,
  user_id uuid NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE,
  content text NOT NULL CHECK (content <> '')
);
COMMENT ON TABLE public.task_comments IS 'Lưu trữ các bình luận cho công việc.';
CREATE INDEX ON public.task_comments (task_id);
CREATE INDEX ON public.task_comments (user_id);


-- 8. HÀM HELPER VÀ TRIGGER
-- Hàm trigger này tự động cập nhật cột `updated_at` mỗi khi một công việc được chỉnh sửa.
CREATE OR REPLACE FUNCTION public.moddatetime()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = now();
    RETURN NEW;
END;
$$ language 'plpgsql';

CREATE TRIGGER handle_updated_at BEFORE UPDATE ON public.tasks
  FOR EACH ROW EXECUTE PROCEDURE public.moddatetime();

-- Hàm trigger này tự động tạo một hồ sơ trong bảng `profiles` khi một người dùng mới đăng ký.
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER
LANGUAGE plpgsql
SECURITY DEFINER SET search_path = public
AS $$
BEGIN
  INSERT INTO public.profiles (id, full_name, avatar_url, role)
  VALUES (new.id, new.raw_user_meta_data->>'full_name', new.raw_user_meta_data->>'avatar_url', 'employee');
  RETURN new;
END;
$$;

CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE PROCEDURE public.handle_new_user();


-- 9. ROW LEVEL SECURITY (RLS) - BẢO MẬT
-- Bật RLS cho tất cả các bảng.
ALTER TABLE public.profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.tasks ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.task_attachments ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.task_time_logs ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.activity_logs ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.task_comments ENABLE ROW LEVEL SECURITY; 

-- Hàm helper để kiểm tra vai trò admin
CREATE OR REPLACE FUNCTION public.is_admin()
RETURNS boolean
LANGUAGE sql
SECURITY DEFINER
AS $$
  SELECT EXISTS (
    SELECT 1
    FROM public.profiles
    WHERE id = auth.uid() AND role = 'admin'
  );
$$;

-- Chính sách cho bảng `profiles`
CREATE POLICY "Người dùng có thể xem hồ sơ của chính họ." ON public.profiles
  FOR SELECT USING (auth.uid() = id);
CREATE POLICY "Người dùng có thể cập nhật hồ sơ của chính họ." ON public.profiles
  FOR UPDATE USING (auth.uid() = id);
CREATE POLICY "Người dùng có thể tự chèn hồ sơ của mình." ON public.profiles
  FOR INSERT WITH CHECK (auth.uid() = id);
CREATE POLICY "Admin có thể quản lý tất cả hồ sơ." ON public.profiles
  FOR ALL USING (public.is_admin());

-- Chính sách cho bảng `tasks`
CREATE POLICY "Người dùng có thể tạo công việc."
ON public.tasks FOR INSERT
WITH CHECK (auth.uid() = created_by);
CREATE POLICY "Người dùng có thể xem, cập nhật, xóa công việc được giao hoặc do họ tạo."
ON public.tasks FOR SELECT, UPDATE, DELETE
USING (auth.uid() = user_id OR auth.uid() = created_by);
CREATE POLICY "Admin có thể quản lý tất cả các công việc." ON public.tasks
  FOR ALL USING (public.is_admin());

-- Chính sách cho bảng `task_attachments`
CREATE POLICY "Người dùng có thể quản lý tệp đính kèm của công việc của họ." ON public.task_attachments
  FOR ALL USING (auth.uid() = user_id);
CREATE POLICY "Admin có thể quản lý tất cả tệp đính kèm." ON public.task_attachments
  FOR ALL USING (public.is_admin());

-- Chính sách cho bảng `task_time_logs`
CREATE POLICY "Người dùng có thể quản lý các bản ghi thời gian của chính họ."
ON public.task_time_logs FOR ALL
USING (auth.uid() = user_id);
CREATE POLICY "Admin có thể quản lý tất cả các bản ghi thời gian."
ON public.task_time_logs FOR ALL
USING (public.is_admin());

-- Chính sách cho bảng `activity_logs`
CREATE POLICY "Người dùng đã xác thực có thể xem tất cả logs." ON public.activity_logs
  FOR SELECT USING (auth.role() = 'authenticated');
CREATE POLICY "Người dùng có thể tạo log cho hành động của chính họ." ON public.activity_logs
  FOR INSERT WITH CHECK (auth.uid() = user_id);
CREATE POLICY "Admin có thể quản lý tất cả logs." ON public.activity_logs
  FOR ALL USING (public.is_admin());

-- Chính sách cho bảng `task_comments`
CREATE POLICY "Người dùng đã xác thực có thể xem tất cả bình luận." ON public.task_comments
  FOR SELECT USING (auth.role() = 'authenticated');
CREATE POLICY "Người dùng có thể thêm bình luận." ON public.task_comments
  FOR INSERT WITH CHECK (auth.uid() = user_id);
CREATE POLICY "Người dùng có thể sửa bình luận của chính họ." ON public.task_comments
  FOR UPDATE USING (auth.uid() = user_id);
CREATE POLICY "Người dùng có thể xóa bình luận của chính họ." ON public.task_comments
  FOR DELETE USING (auth.uid() = user_id);
CREATE POLICY "Admin có thể quản lý tất cả bình luận." ON public.task_comments
  FOR ALL USING (public.is_admin());


-- 10. STORAGE BUCKETS
-- Thiết lập Storage cho avatar và file đính kèm
INSERT INTO storage.buckets (id, name, public)
VALUES ('avatars', 'avatars', true), ('task-attachments', 'task-attachments', true)
ON CONFLICT (id) DO NOTHING;

-- Chính sách cho Storage 'avatars'
CREATE POLICY "Public có thể xem avatars." ON storage.objects
  FOR SELECT USING ( bucket_id = 'avatars' );
CREATE POLICY "Người dùng có thể quản lý avatar của chính họ." ON storage.objects
  FOR ALL USING ( bucket_id = 'avatars' AND auth.uid()::text = (storage.foldername(name))[1] )
  WITH CHECK ( bucket_id = 'avatars' AND auth.uid()::text = (storage.foldername(name))[1] );
CREATE POLICY "Admin có thể quản lý tất cả avatars." ON storage.objects
  FOR ALL USING ( public.is_admin() ) WITH CHECK ( public.is_admin() );

-- Chính sách cho Storage 'task-attachments'
CREATE POLICY "Người dùng có thể quản lý tệp đính kèm của họ." ON storage.objects
  FOR ALL USING ( bucket_id = 'task-attachments' AND auth.uid()::text = (storage.foldername(name))[1] )
  WITH CHECK ( bucket_id = 'task-attachments' AND auth.uid()::text = (storage.foldername(name))[1] );
CREATE POLICY "Admin có thể quản lý tất cả các tệp đính kèm." ON storage.objects
  FOR ALL USING ( bucket_id = 'task-attachments' AND public.is_admin() )
  WITH CHECK ( bucket_id = 'task-attachments' AND public.is_admin() );

```

### 3. Storage Setup

1.  Go to the **Storage** section in your Supabase dashboard.
2.  Create a new bucket named `avatars`. **Make it a Public bucket.**
3.  Create another new bucket named `task-attachments`. **Make it a Public bucket.**

*Note: The SQL script in step 2 attempts to create these buckets, but it's good practice to ensure they exist via the UI.*

### 4. Connect Your Project

Finally, open the `lib/supabase.ts` file in the editor and replace the placeholder credentials with your own **Project URL** and **anon key**.